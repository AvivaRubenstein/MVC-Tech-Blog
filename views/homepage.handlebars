
<div class="container">
  <div class="row">
    {{#each posts as |post|}}
    <div class="col-4">
      <div class="card ">
        <div class="card-body posts" >
    <p class="post-title">{{post.title}}</p>
      <p class="post-content">{{post.content}}</p>
      <p>By {{post.user.name}} on {{format_date post.date_created}}</p>
      {{!-- <button type ="submit" class="btn btn-info" data-id="{{post.id}}"><a href="/post/{{post.id}}">View Full Post</a></button> --}}
      <button type="submit" class="post-list btn" data-id="{{post.id}}">View full post</button>
   </div>
   </div>
    </div>
    {{/each}}
  </div>
</div>
<script src="./js/homepage.js"></script>