
<div class="container">
  <div class="row">
    {{#each posts as |post|}}
    <div class="col">
    <p>{{post.title}}</p>
      <p>{{post.content}}</p>
      <p>By {{post.user.name}} on {{format_date post.date_created}}</p>
    </div>
    {{/each}}
  </div>
</div>